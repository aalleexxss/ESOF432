(function (global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = factory(require('@theme-ui/css')) :
  typeof define === 'function' && define.amd ? define(['@theme-ui/css'], factory) :
  (global = global || self, global.parseProps = factory(global.css));
}(this, (function (css) {
  var getCSS = function getCSS(props) {
    return function (theme) {
      var styles = css.css(props.sx)(theme);
      var raw = typeof props.css === 'function' ? props.css(theme) : props.css;
      return [styles, raw];
    };
  };

  var parseProps = function parseProps(props) {
    if (!props || !props.sx && !props.css) return props;
    var next = {};

    for (var key in props) {
      if (key === 'sx') continue;
      next[key] = props[key];
    }

    next.css = getCSS(props);
    return next;
  };

  return parseProps;

})));
//# sourceMappingURL=index.umd.js.map
